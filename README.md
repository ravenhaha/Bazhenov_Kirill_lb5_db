# Домашнее задание 6 — Работа с БД в Python (CRUD)

**Дисциплина:** Базы данных  
**Тема:** Работа с БД в веб-приложениях  
**Преподаватель:** Михаил Меркушов  

Проект содержит Python-скрипт `crud_app.py`, выполняющий CRUD-операции  
(Create, Read, Update, Delete) с базой данных PostgreSQL.

---

## 📌 Подготовка среды (важно!)

Перед запуском проекта необходимо выполнить подготовку:

### 1. Создать таблицу `users`
Выполните SQL-скрипт:
```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL
);
```

### 2. Ввести свои данные в crud_app.py
```py
conn = psycopg2.connect(
    dbname="postgres",          # ваше имя БД
    user="postgres",            # ваш пользователь PostgreSQL
    password="your_password",   # ваш пароль
    host="localhost",           # чаще всего localhost
    port="5432"                 # порт PostgreSQL
)
```
